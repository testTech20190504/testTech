{% extends 'default.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-md-12 text-center">
            <div class="alert alert-danger">
                Error ! {% if errorMessage %} {{ errorMessage }} {% endif %}
            </div>
        </div>
    </div>
{% endblock %}

